<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"          
             xmlns:data="clr-namespace:AnotadorGymApp.Data.Models.Entities;assembly=AnotadorGymApp.Data"
             xmlns:local="clr-namespace:AnotadorGymApp.RutinasPage"
             x:Name="ThisPage"
             x:Class="AnotadorGymApp.RutinasPage.AgregarRutinaPage"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False">
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <local:IntToTimeSpanConverter x:Key="IntToTimeSpanConverter" />
        </ResourceDictionary>
        
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Spacing="5" Padding="15">
            <!-- TÍTULO PRINCIPAL -->
            <Label Text="🏗️ Crear Nueva Rutina"
                   FontSize="18" FontAttributes="Bold"
                   HorizontalOptions="Center"                   
                   Margin="0,0,0,10"/>

            <!-- CAMPO NOMBRE RUTINA -->
            <Border Style="{StaticResource BorderWithShadowStyle}"
                    Padding="20"
                    Margin="0,0,0,15">

                <StackLayout>
                    <Label Text="📝 Nombre de la Rutina"
                           FontSize="14" FontAttributes="Bold"
                           TextColor="{DynamicResource GreenDarkest}"
                           Margin="0,0,0,8"/>

                    <Entry x:Name="NombreRutinaEntry" 
                           Placeholder="Ej: Rutina Fuerza 5x5, Volumen Hipertrofia..."
                           FontSize="14"
                           TextColor="{DynamicResource PrimaryTextColor}"
                           PlaceholderColor="{DynamicResource DisabledTextColor}"
                           BackgroundColor="Transparent"                                                      
                           Text="{Binding RutinaActual.Nombre,Mode=TwoWay}"/>
                </StackLayout>
            </Border>

            <!-- CAMPO DESCRIPCIÓN RUTINA -->
            <Border Style="{StaticResource BorderWithShadowStyle}"
                    Padding="20"
                    Margin="0,0,0,15">

                <StackLayout>
                    <Label Text="📋 Descripción de la Rutina"
                           FontSize="14" FontAttributes="Bold"
                           TextColor="{DynamicResource GreenDarkest}"
                           Margin="0,0,0,8"/>

                    <Editor x:Name="DescripcionRutinaEntry" 
                            Placeholder="Ej: Rutina de hipertrofia con frecuencia 2, enfoque en ejercicios compuestos..."
                            FontSize="14"
                            TextColor="{DynamicResource PrimaryTextColor}"
                            PlaceholderColor="{DynamicResource DisabledTextColor}"
                            BackgroundColor="Transparent"
                            AutoSize="TextChanges"
                            Text="{Binding RutinaActual.Descripcion,Mode=TwoWay}"/>
                </StackLayout>
            </Border>

            <!-- SELECTOR DE IMAGEN RUTINA -->
            <Border Style="{StaticResource BorderWithShadowStyle}"
                    Padding="20"
                    Margin="0,0,0,15">

                <Grid ColumnDefinitions="*, Auto" 
                      ColumnSpacing="15"
                      VerticalOptions="Center">

                    <!-- TÍTULO Y BOTÓN A LA IZQUIERDA -->
                    <StackLayout Grid.Column="0" VerticalOptions="Center">
                        <Label Text="🖼️ Imagen de la Rutina"
                               FontSize="14" FontAttributes="Bold"
                               TextColor="{DynamicResource GreenDarkest}"
                               Margin="0,0,0,2"/>

                        <Label Text="Seleccionar imagen..."
                               FontSize="12"
                               TextColor="{DynamicResource GreenDarker}"
                               TextDecorations="Underline"
                               HorizontalOptions="Start">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="AgregarImagenButton_Clicked"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </StackLayout>

                    <!-- IMAGEN A LA DERECHA -->
                    <Border Grid.Column="1"
                            Stroke="{DynamicResource GreenDarker}"
                            StrokeThickness="2"
                            StrokeShape="RoundRectangle 8"
                            HeightRequest="80"
                            WidthRequest="80"
                            HorizontalOptions="End"
                            VerticalOptions="Center">

                        <Image Source="{Binding RutinaActual.ImageSource}"
                               Aspect="AspectFill"
                               HeightRequest="76"
                               WidthRequest="76"/>

                    </Border>
                </Grid>
            </Border>

            <!-- SELECTOR DE SEMANAS -->
            <Border Style="{StaticResource BorderWithShadowStyle}"
                    Padding="20">
                <Grid RowDefinitions="Auto, Auto,Auto" RowSpacing="10">
                    
                    <!-- TÍTULO Y DESCRIPCIÓN -->
                    <StackLayout Grid.Row="0" Grid.ColumnSpan="2">
                        <Label Text="📅 Duración de la Rutina"
                               FontSize="14" FontAttributes="Bold"
                               TextColor="{DynamicResource GreenDarkest}"/>
                    </StackLayout>

                    <!-- PICKER ESTILIZADO -->
                    <Border Grid.Row="1" Grid.Column="1" 
                            Style="{DynamicResource BorderWithOutShadowStyle}"
                            Padding="15,10">

                        <Grid ColumnDefinitions="*,Auto,Auto" Padding="0">
                            <Label Text="Selecciona cuántas semanas tendrá tu rutina" Grid.Column="0"
                                   FontSize="12"
                                   TextColor="{DynamicResource SecondaryTextColor}"/>   
                            <Picker x:Name="SemanaPicker"
                                    Grid.Column="1"
                                    ItemsSource="{Binding OpcionesSemanas}"
                                    SelectedItem="{Binding SemanaIndexSeleccionado,Mode=TwoWay}"
                                    FontSize="14"
                                    TextColor="{DynamicResource GreenDark}"
                                    BackgroundColor="Transparent"
                                    WidthRequest="40"/>

                            <!-- ÍCONO PERSONALIZADO -->
                            <Label Grid.Column="2"
                                   Text="⌄"
                                   FontSize="16"
                                   TextColor="{DynamicResource GreenMedium}"
                                   VerticalOptions="Center"
                                   HorizontalOptions="Center"
                                   Margin="0"/>
                        </Grid>
                    </Border>

                    <!-- INDICADOR VISUAL -->
                    <StackLayout Grid.Row="2"
                                 Orientation="Horizontal" 
                                 Spacing="5">
                        <BoxView HeightRequest="3"
                                 WidthRequest="20"
                                 BackgroundColor="{DynamicResource GreenMedium}"
                                 CornerRadius="2"
                                 VerticalOptions="Center"/>     

                        <Label Text="{Binding Source={x:Reference SemanaPicker}, Path=SelectedItem, StringFormat='{0} semanas programadas'}"
                                FontSize="12"
                                TextColor="{DynamicResource SecondaryTextColor}"
                                VerticalOptions="Center"/>
                    </StackLayout>
                </Grid>
            </Border>

            <CollectionView x:Name="SemanasCollectionView" ItemsSource="{Binding RutinaActual.Semanas,Mode=TwoWay}" ItemsUpdatingScrollMode="KeepScrollOffset" Margin="0,10">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="data:RutinaSemana">
                        <StackLayout Padding="0" Spacing="15">
                            <!-- Cabecera: ID + botón mostrar/ocultar -->
                            <Grid ColumnDefinitions="*,Auto" RowDefinitions="40">
                                <Label Text="{Binding NumeroSemana,StringFormat='Semana: {0}'}"                                                                              
                                       VerticalOptions="Center"
                                       Grid.Column="0" />

                                <Button Text="Mostrar" Command="{Binding BindingContext.ItemSeleccionadoCommand, Source={x:Reference Name=ThisPage}}" CommandParameter="{Binding .}"
                                        Grid.Column="1"
                                        VerticalOptions="Center">
                                    <Button.Triggers>
                                        <DataTrigger TargetType="Button" Value="True" Binding="{Binding Seleccionado}">
                                            <Setter Property="Text" Value="Ocultar"></Setter>
                                        </DataTrigger>
                                    </Button.Triggers>
                                </Button>
                            </Grid>

                            <StackLayout IsVisible="{Binding Seleccionado}" Padding="0">
                                <CollectionView x:Name="DiasCollectionView" ItemsSource="{Binding Dias}" VerticalScrollBarVisibility="Never">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate x:DataType="data:RutinaDia">
                                            <Border Padding="15,10" Margin="0,0,0,10" Style="{DynamicResource BorderWithOutShadowStyle}" BackgroundColor="{DynamicResource CardBackground}"
                                                    StrokeThickness="3" IsVisible="True">
                                                <Border.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding BindingContext.ItemSeleccionadoCommand, Source={x:Reference Name=ThisPage}}"
                                                                          CommandParameter="{Binding .}">
                                                    </TapGestureRecognizer>
                                                </Border.GestureRecognizers>
                                                
                                                <VerticalStackLayout Padding="0" BackgroundColor="Transparent">
                                                    <Grid ColumnDefinitions="Auto, *, Auto"
                                                          RowDefinitions="Auto"
                                                          ColumnSpacing="15"
                                                          VerticalOptions="Center">

                                                        <!-- BADGE DEL DÍA -->
                                                        <Border Grid.Column="0"
                                                                BackgroundColor="{DynamicResource GreenMedium}"
                                                                Stroke="Transparent"
                                                                StrokeThickness="0"
                                                                StrokeShape="RoundRectangle 20"
                                                                Padding="12,8"
                                                                VerticalOptions="Center">

                                                            <HorizontalStackLayout Spacing="5" VerticalOptions="Center">
                                                                <Label Text="📅"
                                                                        FontSize="12"
                                                                        
                                                                        VerticalOptions="Center"/>
                                                                <Label Text="{Binding NumeroDia}"
                                                                       FontSize="12"
                                                                       FontAttributes="Bold"
                                                                       
                                                                       VerticalOptions="Center"/>
                                                            </HorizontalStackLayout>
                                                        </Border>

                                                        <!-- ENTRADA DEL NOMBRE -->
                                                        <Entry     Text="{Binding NombreRutinaDia}" 
                                                                   Placeholder="Ej: Lunes - Pecho, Martes - Pierna..."
                                                                   PlaceholderColor="{DynamicResource SecondaryTextColor}"
                                                                   TextColor="{DynamicResource PrimaryTextColor}"
                                                                   FontSize="14"
                                                                   BackgroundColor="Transparent"
                                                                   ClearButtonVisibility="WhileEditing"
                                                                   Grid.Column="1"
                                                                   VerticalOptions="Center"
                                                                   TextChanged="NombreDiaEntry_TextChanged"/>

                                                        <!-- BOTÓN ELIMINAR -->
                                                        <Button Grid.Column="2"
                                                                Style="{x:StaticResource DeleteButtonStyle}"
                                                                CommandParameter="Dia" 
                                                                Clicked="EliminarElemento_Clicked" 
                                                                VerticalOptions="Center"/>

                                                    </Grid>
                                                    
                                                    <CollectionView x:Name="EjerciciosCollectionView" ItemsSource="{Binding Ejercicios}"
                                                                    Margin="0" VerticalScrollBarVisibility="Never" IsVisible="{Binding Seleccionado}">
                                                        <CollectionView.ItemTemplate>
                                                            <DataTemplate x:DataType="data:RutinaEjercicio">
                                                                <VerticalStackLayout Padding="0" Margin="0" Spacing="0" BackgroundColor="Transparent">
                                                                    <VerticalStackLayout.GestureRecognizers>
                                                                        <TapGestureRecognizer Command="{Binding BindingContext.ItemSeleccionadoCommand, Source={x:Reference Name=ThisPage}}"
                                                                                                CommandParameter="{Binding .}">
                                                                        </TapGestureRecognizer>
                                                                    </VerticalStackLayout.GestureRecognizers>

                                                                    <Grid RowDefinitions="Auto,Auto,Auto,Auto" ColumnDefinitions="*,*" HorizontalOptions="Fill" VerticalOptions="Start" RowSpacing="0">
                                                                                                                                                
                                                                        <Label Text="{Binding Ejercicio.Nombre}" Grid.Row="0"
                                                                                LineBreakMode="WordWrap"
                                                                                VerticalOptions="Center" 
                                                                                VerticalTextAlignment="Center"
                                                                                Style="{DynamicResource SubHeadline}"/>
                                                                        <Button Style="{x:StaticResource DeleteButtonStyle}" Grid.Row="0" Grid.Column="1" HorizontalOptions="Start"
                                                                                    CommandParameter="Ejercicio" 
                                                                                    Clicked="EliminarElemento_Clicked"                                                                                     
                                                                                    VerticalOptions="Center"/>

                                                                        <CollectionView ItemsSource="{Binding Series}" VerticalScrollBarVisibility="Never" IsVisible="{Binding Seleccionado}" Grid.Row="2" Grid.ColumnSpan="2">
                                                                            <CollectionView.ItemsLayout>
                                                                                <LinearItemsLayout ItemSpacing="10" Orientation="Vertical"></LinearItemsLayout>
                                                                            </CollectionView.ItemsLayout>
                                                                            <CollectionView.ItemTemplate>
                                                                                <DataTemplate x:DataType="data:RutinaSeries">
                                                                                    <Border Padding="0" Margin="0" StrokeThickness="2">
                                                                                        <Grid RowSpacing="0" RowDefinitions="Auto,Auto,Auto,Auto,Auto" ColumnDefinitions="Auto,*" Padding="20" Margin="0">

                                                                                            <HorizontalStackLayout Grid.Row="0" Grid.ColumnSpan="2" HeightRequest="30" Spacing="7" Padding="0" Margin="0">
                                                                                                <Label Text="Serie " Style="{DynamicResource SubHeadline}" VerticalOptions="Center" />
                                                                                                <Label Text="{Binding NumeroSerie}" Style="{DynamicResource SubHeadline}" VerticalOptions="Center"/>
                                                                                                <Button Style="{x:StaticResource DeleteButtonStyle}" BorderWidth="1"
                                                                                                        CommandParameter="Serie" Clicked="EliminarElemento_Clicked" HorizontalOptions="End" VerticalOptions="Center"/>
                                                                                            </HorizontalStackLayout>

                                                                                            <BoxView Grid.Row="1" Grid.ColumnSpan="2" HeightRequest="2" Margin="0,10,0,20"></BoxView>
                                                                                            
                                                                                            <HorizontalStackLayout Grid.Row="2" Grid.Column="0" Spacing="15" Padding="0">
                                                                                                <HorizontalStackLayout.Triggers>
                                                                                                    <DataTrigger TargetType="HorizontalStackLayout" Binding="{Binding Tipo}" Value="{x:Static data:TipoSerie.Max_Rm}">
                                                                                                        <Setter Property="IsVisible" Value="False"></Setter>
                                                                                                    </DataTrigger>
                                                                                                </HorizontalStackLayout.Triggers>
                                                                                                <Label Text="Reps:" VerticalOptions="Center"/>
                                                                                                <Border Style="{DynamicResource BorderWithOutShadowStyle}" Padding="0" MaximumHeightRequest="40">
                                                                                                    <Entry Text="{Binding Repeticiones,Mode=TwoWay}" Keyboard="Numeric" Visual="Material" 
                                                                                                           HorizontalTextAlignment="Center" VerticalOptions="End" PlaceholderColor="Transparent" MaxLength="2" FontSize="14"/>
                                                                                                </Border>
                                                                                            </HorizontalStackLayout>

                                                                                            <HorizontalStackLayout Grid.Row="2" Spacing="5" Grid.Column="1" IsVisible="True" HorizontalOptions="End">
                                                                                                <HorizontalStackLayout.Triggers>
                                                                                                    <DataTrigger TargetType="HorizontalStackLayout" Binding="{Binding Tipo}" Value="{x:Static data:TipoSerie.Max_Rm}">
                                                                                                        <Setter Property="IsVisible" Value="False"></Setter>
                                                                                                    </DataTrigger>
                                                                                                </HorizontalStackLayout.Triggers>
                                                                                                <Label Text="Carga (%):" VerticalOptions="Center"/>
                                                                                                <Border Style="{DynamicResource BorderWithOutShadowStyle}" Padding="0" MaximumHeightRequest="40">
                                                                                                    <Entry Text="{Binding Porcentaje1RM,Mode=TwoWay}" WidthRequest="30" Keyboard="Numeric" Visual="None" MaxLength="2" HorizontalTextAlignment="Center"/>
                                                                                                </Border>
                                                                                            </HorizontalStackLayout>

                                                                                            <HorizontalStackLayout Grid.Row="3" Spacing="2" Grid.ColumnSpan="2">
                                                                                                <HorizontalStackLayout.Triggers>
                                                                                                    <DataTrigger TargetType="HorizontalStackLayout" Binding="{Binding Tipo}" Value="{x:Static data:TipoSerie.Max_Rm}">
                                                                                                        <Setter Property="IsVisible" Value="False"></Setter>
                                                                                                    </DataTrigger>
                                                                                                    <DataTrigger TargetType="HorizontalStackLayout" Binding="{Binding Tipo}" Value="{x:Static data:TipoSerie.DropSet}">
                                                                                                        <Setter Property="IsVisible" Value="False" />
                                                                                                    </DataTrigger>
                                                                                                </HorizontalStackLayout.Triggers>
                                                                                                <Label Text="Descanso (S): " VerticalOptions="Center"/>
                                                                                                <Picker Title="Segundos..."                                                                                                 
                                                                                                        ItemsSource="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}},Path=OpcionesSegundos}"                                                                                                
                                                                                                        SelectedItem="{Binding Descanso}"
                                                                                                        ItemDisplayBinding="{Binding ., Converter={StaticResource IntToTimeSpanConverter}}"                                                                                                
                                                                                                        VerticalOptions="End"/>
                                                                                                <Label Grid.Column="1"
                                                                                                       Text="▼"
                                                                                                       FontSize="16"
                                                                                                       VerticalOptions="Center"
                                                                                                       Margin="0,0,10,0"
                                                                                                       TextColor="{StaticResource SecondaryTextColor}" />
                                                                                            </HorizontalStackLayout>

                                                                                            <HorizontalStackLayout Grid.Row="4" Spacing="5" Grid.ColumnSpan="2">
                                                                                                <Label Text="Tipo Serie: " VerticalOptions="Center"/>
                                                                                                <Picker Title="Tipo Serie" ItemsSource="{Binding Source={x:Reference ThisPage},Path=BindingContext.TipoSerieEnum}" SelectedItem="{Binding Tipo,Mode=TwoWay}"/>
                                                                                                <Label Grid.Column="1"
                                                                                                        Text="▼"
                                                                                                        FontSize="16"
                                                                                                        VerticalOptions="Center"
                                                                                                        Margin="0,0,10,0"
                                                                                                        TextColor="{StaticResource SecondaryTextColor}" />
                                                                                            </HorizontalStackLayout>

                                                                                        </Grid>
                                                                                    </Border>

                                                                                </DataTemplate>
                                                                            </CollectionView.ItemTemplate>
                                                                        </CollectionView>
                                                                        
                                                                        <Button Text="Agregar Serie" CommandParameter="Serie" Clicked="AgregarElemento_Clicked" Grid.Row="3" Grid.ColumnSpan="2"
                                                                                HeightRequest="40" HorizontalOptions="Center" VerticalOptions="Start" Margin="0,6" IsVisible="{Binding Seleccionado}"></Button>
                                                                    </Grid>
                                                                    
                                                                </VerticalStackLayout>
                                                            </DataTemplate>
                                                        </CollectionView.ItemTemplate>
                                                    </CollectionView>
                                                    
                                                    <Button x:Name="AgregarEjercicioButton" Text="Agregar Ejercicio" Clicked="AgregarElemento_Clicked" CommandParameter="Ejercicio" Margin="0,10" IsVisible="{Binding Seleccionado}"/>
                                                </VerticalStackLayout>
                                            </Border>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>
                                <Button x:Name="AgregarDiaButton" Text="Agregar Día" Clicked="AgregarElemento_Clicked" CommandParameter="Dia"/>
                            </StackLayout>
                            
                            <BoxView HeightRequest="1" Margin="0,5"/>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            
            <Button x:Name="GuardarRutinaButton" Text="Guardar Rutina" Clicked="GuardarRutinaButton_Clicked" VerticalOptions="End"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
